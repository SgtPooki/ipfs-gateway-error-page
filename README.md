# ipfs-gateway-error-page

## Installation

```bash
pip install -r requirements.txt
```

## Getting started

Modify `app.py` with the template you want to edit in the `home` function. Then run `python app.py` to start the server.
